DATABASE_URL=postgresql://user:password@localhost:5432/autoconnect

ENVIRONMENT=development

TELEMETRY_DEVICE_SECRET=your_device_secret

# Optional — enables real LLM-generated vehicle insights.
# Without it, /vehicles/{id}/ai-insight uses a rule-based fallback.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
